{% extends 'base.html.twig' %}

{% block title %}{{ titre }}{% endblock %}

{% block body %}
<h1>{{ titre }}</h1>

{{ form_start(form) }}
    {{ form_row(form.titre_plat) }}
    {{ form_row(form.note) }}{% extends 'base.html.twig' %}

{% block title %}{{ titre }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/pages/admin.css') }}">
{% endblock %}

{% block body %}
<div class="container py-5">

    {# Titre #}
    <div class="mb-4">
        <h1>{{ titre }}</h1>
        <div class="separateur-or mt-2"></div>
    </div>

    {# Formulaire #}
    <div class="carte-sombre p-4">
        {{ form_start(form) }}
            <div class="row g-3">
                <div class="col-md-6">
                    {{ form_row(form.titre_plat) }}
                </div>
                <div class="col-md-6">
                    {{ form_row(form.note) }}
                </div>
                <div class="col-12">
                    {{ form_row(form.allergenes) }}
                </div>
            </div>

            <div class="d-flex gap-3 mt-4">
                <button type="submit" class="btn bouton-or">
                    <i class="bi bi-check-lg me-2"></i>Enregistrer
                </button>
                <a href="{{ path('app_admin_plats') }}" class="btn bouton-secondaire">
                    <i class="bi bi-arrow-left me-2"></i>Retour
                </a>
            </div>
        {{ form_end(form) }}
    </div>

</div>
{% endblock %}
    {{ form_row(form.allergenes) }}

    <button type="submit">Enregistrer</button>
{{ form_end(form) }}

<a href="{{ path('app_admin_plats') }}">← Retour</a>
{% endblock %}