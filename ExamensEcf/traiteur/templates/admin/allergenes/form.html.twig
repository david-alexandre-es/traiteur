{% extends 'base.html.twig' %}

{% block title %}{{ titre }}{% endblock %}

{% block body %}
<h1>{{ titre }}</h1>

{{ form_start(form) }}
    {{ form_row(form.libelle) }}
    <button type="submit">Enregistrer</button>
{{ form_end(form) }}

<a href="{{ path('app_admin_allergenes') }}">← Retour</a>
{% endblock %}